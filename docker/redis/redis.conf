# Durable local configuration for the non-expiring identity catalog.
bind 0.0.0.0
protected-mode no
port 6379

appendonly yes
appendfsync everysec
save 60 1000
dir /data

# Only response and lease keys expire. Preserve non-expiring catalog keys.
maxmemory-policy volatile-lfu

loglevel notice
